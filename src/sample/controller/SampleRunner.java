package sample.controller;

public class SampleRunner 
{	
	public static void main(String [] args)
	{
		SampleController myApp = new SampleController();// Equal signs is not math equal sign. = is assignment operator. One way only (Right to left). New is a keyword. 
		myApp.start();// Constructor has same name as type. 
	}
}
